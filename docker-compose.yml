script:
  build: bootstrap/
  links:
    - db
  environment:
    - PGHOST=db
  volumes:
    - tmp/osmosis/:/tmp/osmosis/
excerpt:
  build: osmaxx_excerpt/
  links:
    - db
  environment:
    - PGHOST=db
db:
  image: geometalab/postgis-with-translit
