## Layer Overview

See file name conventions above about the meaning of “_a” etc.


|Tables        |Geometry Type        |Description                                              |
| ------------ | ------------------- | ------------------------------------------------------- |
{% for layer_name, layer_definition in layers|dictsort_unless_ordered -%}
    |
    {{- layer_name -}}
    |
    {%- if layer_name.endswith('_a') -%}
        MULTIPOLYGON
    {%- elif layer_name.endswith('_l') -%}
        MULTILINESTRING
    {%- elif layer_name.endswith('_p') -%}
        POINT
    {%- endif -%}
    |
    {{- layer_definition['summary'] -}}
    |
{% endfor %}

# Layers Specification

{% for layer_name, layer_definition in layers|dictsort_unless_ordered -%}
    {% include 'layer.md.jinja2' -%}
{%- endfor %}
