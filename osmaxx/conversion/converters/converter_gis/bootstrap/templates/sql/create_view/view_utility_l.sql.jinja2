CREATE OR REPLACE VIEW "{{ view_schema_name }}".utility_l AS SELECT
    osm_id,
    lastchange,
    geomtype,
    geom,
    "type",
    "name",
    name_en,
    name_fr,
    name_es,
    name_de,
    int_name,
    label,
    tags,
    operator,
    voltage,
    frequency
FROM "{{ schema_name }}".utility_l;
